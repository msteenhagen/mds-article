% TODO add fonts to latex
%% Document preamble for articles
%% Mark Sprevak <http://sites.google.com/site/msprevak>
%%
%% Borrows heavily from many other sources, most notably Kieran Healy:
%% <https://github.com/kjhealy/latex-custom-kjh>
%%
%% # Usage
%%
%% This file should be input in the first line of a .tex file, e.g.:
%%
%% \input{article-preamble.tex} 
%% \title{My article title}
%% \begin{document}
%% ...
%% \end{document}
%%
%% The \published command adds publication info:
%% e.g. \published{Forthcoming in the \emph{The Journal}}
%%
%% # Prerequisites #
%%
%% 1. A modern LaTeX installation
%% 2. Minion Pro fonts: <http://goo.gl/lQqMy>
%% 3. Biblatex
%% 4. The `gitinfo` package

\documentclass[oneside,12pt]{article}
\usepackage{fixltx2e}
\usepackage[T1]{fontenc}
\usepackage[a4paper]{geometry}
\geometry{
  top=2.5cm,            % <-- you want to adjust this
  bottom=3.5cm,
  headheight=3ex,       %% <-- and this
  headsep=2ex,          % <-- and this
}
\usepackage[opticals,fullfamily,mathlf,minionint,footnotefigures]{MinionPro}
\usepackage[scaled=0.85]{inconsolata}
\usepackage{booktabs}
\usepackage{graphicx}
\usepackage{setspace} 				% Doublespacing, etc.
\usepackage{xcolor}
\usepackage{url}
\usepackage{enumitem}
\usepackage{fancyhdr}
\usepackage{titling}
\usepackage{gitinfo}
\usepackage[small]{titlesec}
\usepackage[british,cleanlook]{isodate}
\usepackage[british]{babel}
\usepackage[autostyle]{csquotes}
\usepackage[babel]{microtype}

\def\myauthor{Mark Sprevak}
\def\myaffiliation{University of Edinburgh}
\def\myemail{\texttt{\href{mailto:mark.sprevak@ed.ac.uk}{mark.sprevak@ed.ac.uk}}}

%% Bibliography - biblatex 
%%
\usepackage[useprefix,authordate,natbib,backend=biber]{biblatex-chicago}
\setlength\bibitemsep{6pt}	
\addbibresource{refs.bib}

%% Bibliography - natbib 
%%
% \usepackage[round,longnamesfirst,sort]{natbib}
% \setlength{\bibsep}{6pt}


%% Header for first page
%%
%% Center footer:	page number
%% Right header:	email address
%% Left header:		publication info/'this is a draft'
%%
%% Publication set with \published
%%
\pagestyle{fancy}

\newcommand{\published}[1]{\def\publicationinfo{{#1}}}

\fancypagestyle{plain}{
  \fancyhf{}
  \renewcommand{\headrulewidth}{0pt}
	\fancyfoot[C]{\thepage}
	\fancyhead[R]{\footnotesize \myemail}
	\ifdefined\publicationinfo
		\fancyhead[L]{\footnotesize	\publicationinfo}
	\else
		\fancyhead[L]{\footnotesize	\emph{Draft only. Please do not cite without permission.}}
 	\fi
}

%% Headers for all pages but References
%%
%% Center footer:	page number
%% Right header:	subsection title
%% Left header:		section number and title
%%
\renewcommand{\sectionmark}[1]{\markboth{\small \thesection.\ #1}{}} 
\renewcommand{\subsectionmark}[1]{\markright{\small #1}}
\lhead{\nouppercase{\leftmark}}
\rhead{\nouppercase{\rightmark}}
\fancyfoot[C]{\thepage}
	

%% Description labels: bold, small caps, and lowercase
%% Description: forced on next line if label is too long
\renewcommand{\descriptionlabel}[1]{\hspace*{\labelsep}\textsc{\textbf{\MakeLowercase{#1}}}}
\setlist[description]{style=nextline}

%% Section titles: make sub-subsection headings italic
\titleformat{\subsubsection}
{\itshape\normalsize}{\thesubsubsection}{1em}{}

%% URL font matches surrounding text
% \urlstyle{same}

\definecolor{purple_link}{RGB}{76, 64, 133}

%% Force footnotes to stay on the same page
\interfootnotelinepenalty=10000 	

%% Allow text to stretch a little into margins to avoid bad line-breaks
\setlength{\emergencystretch}{3em}  

\usepackage[
	plainpages = false,
	pdfpagelabels,
	bookmarksnumbered,
	pdftex,
	colorlinks = true,
	citecolor  = purple_link,
	linkcolor  = purple_link,
	urlcolor   = purple_link
]{hyperref}

%% Quote environment takes up less space
%% copied from savetrees.sty
\makeatletter
\renewenvironment{quote}
               {\list{}{\listparindent=0pt%whatever you need
                        \itemindent    \listparindent
                        \leftmargin=2em%  whatever you need
                        \rightmargin=2em%whatever you need
                        \topsep=0pt%%%%%  whatever you need
                        \parsep        \z@ \@plus\p@}%
                \item\relax}
               {\endlist}
\makeatother

\def\signed #1{{\leavevmode\unskip\nobreak\hfil\penalty50\hskip2em
  \hbox{}\nobreak\hfil#1%
  \parfillskip=0pt \finalhyphendemerits=0 \endgraf}}

\newsavebox\mybox
\newenvironment{aquote}[1]
  {\savebox\mybox{#1}\begin{quote}}
  {\signed{\usebox\mybox}\end{quote}}


%% Titling 
%%
\pretitle{\par\vskip 3em \begin{flushleft}\LARGE\bfseries}
\posttitle{\end{flushleft}\par\vskip 0.5em}
\preauthor{\begin{flushleft}\normalsize\bigskip}
\postauthor{\end{flushleft}}
\predate{\begin{flushleft}\normalsize}
\postdate{\end{flushleft}}

%% If \gitAuthorDate is set, print it as the date
%% If not, then print '(None)' to complain as the date
%%
\ifnum\pdfstrcmp{\gitAuthorDate}{(None)}=0 
	\date{\gitAuthorDate}
\else
	\date{\printdate{\gitAuthorDate}}
\fi

\author{\myauthor 					\\
  			\emph{\myaffiliation}}

%% Set up a plain abstract environment
%%
\renewenvironment{abstract}{\begin{quote}}{\end{quote}}


%% Inserted by default just after \begin{document}
%%
\AtBeginDocument{

	%% Set author and title metadata in output PDF
	\makeatletter
	  \hypersetup{
	    pdftitle = {\@title},
	    pdfauthor = {\@author}
	  }
	\makeatother
	
	\maketitle
	
	%% Paragraphs don't indent first line
	%% Add small space to separate paragraphs
	\setlength{\parindent}{0pt}
	\setlength{\parskip}{6pt plus 2pt minus 1pt}
}


%% Inserted by default just before \end{document}
%%
\AtEndDocument{

	%% Reset headings for References
	\rhead{}										
	\lhead{\small References}

	%% Bibliography - natbib
	% \bibliographystyle{newapa}
	% \bibliography{refs}

	%% Bibliography - biblatex
	\printbibliography
	
	%% Print git version and git date if set at end, if not do nothing
	\ifnum\pdfstrcmp{\gitAuthorDate}{(None)}=0 
		\relax 
	\else
		\flushright{\sffamily \scriptsize \gitAbbrevHash\ on \gitAuthorDate}
	\fi
}
